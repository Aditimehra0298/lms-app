"use client";

import AdminCoursesWorkspace from "@/components/admin/AdminCoursesWorkspace";

/** Self-paced curriculum editor — Admin → Lessons. */
export default function AdminLessonsWorkspace() {
  return <AdminCoursesWorkspace mode="lessons" />;
}
